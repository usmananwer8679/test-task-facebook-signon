class User {
    constructor(id, displayName, profileUrl, photos) {
      this.id = id;
      this.displayName = displayName;
      this.profileUrl = profileUrl;
      this.photos = photos;
    }
  }
  
  module.exports = User;